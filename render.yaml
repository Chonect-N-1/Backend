services:
  - type: web
    name: chonect-backend
    env: docker
    # Usar imagen de Docker Hub
    image:
      name: carrlox/chonect-backend:latest
    healthCheckPath: /actuator/health
    autoDeploy: true
    # Las variables de entorno se configuran en Render Dashboard
    # No es necesario duplicarlas aquí
